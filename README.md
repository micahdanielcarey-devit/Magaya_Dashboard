Good baseline for a Dashboard.

Handles CSV's exported from Magaya better suited to handle large bulk i.e (From Jan 1st 2025 - August 16th 2026).
Has two static pages, Airlines and Index, airlines handle all carrier information index does the rest.
Things to make note of in the ETL when adding a new CSV either copy the existing name that is being used in the line of code or change the name in the line of code either works I prefer chaning the file name.
Dasboard has filter by date capabilites, room to add filter by name is there as well.
